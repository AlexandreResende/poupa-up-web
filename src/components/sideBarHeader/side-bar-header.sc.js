
import styled from 'styled-components';

const SideBarHeader = styled.div`
  color: white;
  margin: ${props => props.marginTop || '2rem'} auto 2rem auto;
  font-size: 1.3rem;
  font-family: Verdana, Tahoma, sans-serif;
  text-align: center;
`;

export default SideBarHeader;
