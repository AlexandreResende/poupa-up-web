
// reducer example

/* const INITIAL_STATE = [];

export default function expenses(state = INITIAL_STATE, action) {
  switch(action.type) {
    case 'FETCH_INCOME':
      return state;
    default:
      return state;
  }
};
 */